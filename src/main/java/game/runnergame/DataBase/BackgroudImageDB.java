package game.runnergame.DataBase;

import javafx.scene.image.Image;

import java.util.HashMap;
import java.util.Map;

public class BackgroudImageDB {
    public static HashMap<String, Image> backgroundImages = new HashMap<>(Map.of("Green Zone", new Image(("Images/Background.png"))));
}
